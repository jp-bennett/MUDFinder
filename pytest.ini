[pytest]
testpaths = tests

# Note: runs emit one EventletDeprecationWarning. It is raised while eventlet
# is imported, before pytest installs its warning filters, so filterwarnings
# cannot suppress it. It goes away when eventlet does.

markers =
    browser: end-to-end tests that drive a real browser against a live server
